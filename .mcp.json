{
	"$schema": "https://json.schemastore.org/mcp.json",
	"mcpServers": {
		"_comment_notion": "Uncomment and set NOTION_TOKEN in .env to enable the Notion MCP server for Claude Code. The user already has Notion configured globally; this repo-level entry exists as a template for client forks.",
		"_notion_template": {
			"command": "npx",
			"args": ["-y", "@notionhq/notion-mcp-server"],
			"env": {
				"NOTION_TOKEN": "${NOTION_TOKEN}"
			}
		}
	}
}
