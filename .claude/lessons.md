# Lessons learned

Rolling log of patterns surfaced during Claude sessions. The PreCompact hook (`.claude/hooks/append-lesson.sh`) appends a timestamped entry here before the conversation is compacted, so non-obvious findings aren't lost when context is trimmed.

Entries below this line are appended automatically — add manual entries by editing above this note if needed.
